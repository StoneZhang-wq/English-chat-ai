# Railway / 豆包·OpenAI 专用瘦身依赖（无 PyTorch / faster-whisper / Spark-TTS）
# 部署时请设置 FASTER_WHISPER_LOCAL=false, API_PROVIDER=doubao 或 openai
# 安装: pip install -r requirements-railway.txt

# Web framework
fastapi==0.115.11
uvicorn==0.30.1
websockets==15.0.1
python-multipart>=0.0.6

# API clients
openai==1.68.2
anthropic==0.49.0
aiohttp==3.12.14
httpx==0.28.1
requests==2.32.4

# Config & validation
pydantic==2.7.4
python-dotenv==1.0.1

# Audio (for processing uploaded / API audio)
numpy>=1.21.6,<1.28.0
soundfile==0.12.1
pydub==0.25.1

# WebRTC (OpenAI Realtime)
aiortc>=1.14.0
av>=14.0.0

# UI / text
pillow==10.3.0
textblob==0.18.0.post0

# Excel (optional)
pandas==2.2.2
openpyxl==3.1.5

# 以下为可选：若 Railway 构建报错可注释，并确保不触发服务端麦克风录音
# PyAudio==0.2.14
